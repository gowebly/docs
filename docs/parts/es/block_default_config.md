::: warning Uso de una configuración por defecto
Si no ejecutas el comando `init` para crear un archivo de configuración (`.gowebly.yml`), la CLI de **Gowebly** utiliza la configuración [default][repo_default_config].
:::